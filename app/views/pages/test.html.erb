#$ ->
  #$('#locations').imagesLoaded ->
    #$('#locations').masonry
      #itemSelector: '.box'
      #isFitWidth: true
      # columnWidth: 244
      # rowWidth: 300
      # gutter: 20













<%= link_to 'New Location', new_location_path, class: "btn btn-primary btn-sm" %>

function initialize() {
	
	var styleArray = [
    {
      featureType: "all",
      stylers: [
       { saturation: -80 }
      ]
    },{
      featureType: "road.arterial",
      elementType: "geometry",
      stylers: [
        { hue: "#00ffee" },
        { saturation: 50 }
      ]
    },{
      featureType: "poi.business",
      elementType: "labels",
      stylers: [
        { visibility: "off" }
      ]
    }
  ];

<div class="form-inputs">
    <%= f.input :description %>
  </div>

  <%= f.simple_fields_for :paintings do |p| %>
    <%= p.input :image, as: :file %>
  <% end %>

  <div class="form-actions">
    <%= f.button :submit, class: "btn btn-primary btn-lg" %>
  </div>



<script src="http://maps.googleapis.com/maps/api/js"></script>
<script>



	var mapProp = {
    center:new google.maps.LatLng(30.285332,-97.734559),
    zoom:11,
    mapTypeId:google.maps.MapTypeId.ROADMAP
	};
	var map=new google.maps.Map(document.getElementById("googleMap"),mapProp);

	var marker1=new google.maps.Marker({
  position:new google.maps.LatLng(30.295712,-97.705957),
  });

	var marker2=new google.maps.Marker({
	  position:new google.maps.LatLng(30.326498,-97.723357),
	});

	marker1.setMap(map);
	marker2.setMap(map);
}
google.maps.event.addDomListener(window, 'load', initialize);

/////////////////////////////////////////////////////////////////////////////////

function initialize() 
{
  // Creates the map object.
  var myCenter = new google.maps.LatLng(30.296632,-97.704021);
  var mapProp = {
      center: myCenter,
      zoom:11,
      mapTypeId:google.maps.MapTypeId.ROADMAP
  };
  var map = new google.maps.Map(document.getElementById("gmap"),mapProp);



  var infowindow = new google.maps.InfoWindow({
        content: "Location 1"
  });

  var name = 'location1';

  var marker = new google.maps.Marker({
          position: myCenter,
          map: map
  });

  google.maps.event.addListener(marker, 'click', function() {
      infowindow.open(map,marker);
      map.setCenter(myCenter);
  });

  google.maps.event.addDomListener(document.getElementById(name), 'click', function() {
      infowindow.open(map,marker);
      map.setCenter(myCenter);
  });
}


google.maps.event.addDomListener(window, 'load', initialize);